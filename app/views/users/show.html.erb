<h1>Users#show</h1>

<p>
  <strong>email:</strong>
  <%= @user.email %>
</p>

<table>
  <% @posts.each do |post| %>
    <tr>
        <td><%= post.content %></td>
        <td><%= post.tags %></td>
        <td><%= post.created_at %></td>
        <td><%= link_to 'Show', post_path(post) %></td>
    </tr>
  <% end %>
</table>